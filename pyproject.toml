[project]
name = "ainews-clustering"
version = "0.1.0"
description = "News dataset exploration with clustering"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pandas>=2.0.0",
    "pyarrow>=14.0.0",
    "ipython>=8.0.0",
    "scikit-learn>=1.5.2",
]
